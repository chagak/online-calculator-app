variables:
  buildConfiguration: 'Release'

steps:
- task: DotNetCoreCLI@2
  inputs:
    command: 'publish'
    publishWebProjects: true
- task: AzureWebApp@1
  inputs:
    azureSubscription: 'Towards-azurePortal'
    appType: 'webAppLinux'
    appName: 'webcalculatorchaga-app'
    package: '$(System.DefaultWorkingDirectory)/**/*.zip'